services:
  users-test-db:
    image: postgres:15-alpine
    container_name: ecommerce-users-test-db
    environment:
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_password
      POSTGRES_DB: users_service_test
    ports:
      - "5442:5432"
